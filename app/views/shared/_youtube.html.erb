<% unless video_url.blank? %>
    <div style="text-align:center">
      <iframe width="<%= width %>" height="<%= height %>" src="<%= video_url -%>?feature=oembed" frameborder="0" allowfullscreen>
      </iframe>
    </div>
<% end %>