.row.form-row
  .col-xs-4.col-md-4.form-label
    %b.pull-right
      - if header.blank?
        &nbsp;
      - else
        = header
  .col-xs-8.col-md-8.form-value{:id => "#{header.parameterize}"}
    - unless value.blank?
      = value
    - else
      .blank
        Blank
