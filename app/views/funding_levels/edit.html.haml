.container
  .row
    .col-md-8
      = form_for [@campaign, @investment, @funding_level], :url => campaign_investment_funding_level_path(@campaign, @investment), :html => {:class => 'form-horizontal well'} do |f|

        = render 'shared/errors', :entity => @funding_level
        = render_cell "tb/form/subhead", :display, :label => "Funding Level"

        %fieldset
          = f.hidden_field :investment_id
          = render_cell 'tb/form/text', :display, :form => f, :field => :amount
          = render_cell 'tb/form/submit_button', :display

    .col-md-4
      = render :partial => 'companies/raise_summary', :locals => {:just_data => true}
