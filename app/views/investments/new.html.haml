.container
  = render 'campaigns/heading'
  -#= render 'campaigns/navigation', :active => :none

  .row
    .col-md-8
      = render 'companies/heading'
      %h3
        Begin investment process
      = render 'form'

    .col-md-4
      = render :partial => 'companies/raise_summary'