%td
  = "An investment of #{number_to_currency(event.investment.amount)} was funded"
%td
  = link_to event.investment.investor.user.full_name, user_path(event.investment.investor.user)
%td
  = link_to 'Bancbox Investments', bancbox_investments_user_admin_path(:investor_id => event.investment.investor.id), :class => 'btn btn-primary btn-xs'
%td
  = link_to campaign.company.campaign_title, display_campaign_company_path(campaign)