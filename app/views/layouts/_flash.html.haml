- flash.each do |type, message|
  %div{:id => "alert-#{type}", :class => "alert alert-#{type} alert-dismissable fade in"}
    %button{:type => "button", :class => "close", "data-dismiss" => "alert", "aria-hidden" => "true"}
      &times;
    = message
