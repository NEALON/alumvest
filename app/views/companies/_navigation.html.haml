= content_for :breadcrumbs do
  %ul.breadcrumbs
    %li= link_to('Home', '/')
    %li.unavailable= 'Business owner'
    %li= link_to(company.owner.user.full_name, user_path(company.owner.user))
    %li.current= link_to((company.company_name || 'New company'), company_path(company))

= content_for :header_1 do
  %h4
    = company.company_name || 'New company'

  %p
    You can fill in whatever information you have for each section, not including Updates, and save your work. When you are ready, push the "Check for completeness" button for each section, to see what mandatory information you may be missing.
  %p
    When you have completed the Company, Team, and Investment Terms sections, and you are ready to submit your company for review by our site administators, click the button below. Your company information will not be editable while it is under review.
  %p
    = link_to 'Submit for review', company_submit_for_review_path(company), :method => :post, :class => 'button small'
  %hr

= content_for :sidebar_left do
  %ul.large-12.small-12.side-nav
    %li{:class => "#{'active' if active == :demographic}"}
      %h4= link_to('Company', company_path(company))

    %li{:class => "#{'active' if active == :people}"}
      - unless entity = company.team
        %h4= link_to('Team', new_company_team_path(company))
      - else
        %h4= link_to('Team', company_team_path(company, entity))
    %li{:class => "#{'active' if active == :funding}"}
      - unless entity = company.investment_term
        %h4= link_to('Investment Terms', new_company_investment_term_path(company))
      - else
        %h4= link_to('Investment Terms', company_investment_term_path(company, entity))
    %li{:class => "#{'active' if active == :updates}"}
      %h4= link_to('Updates', company_updates_path(company))