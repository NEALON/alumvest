= render 'navigation', :active => @active
= render "users/header"

#information
  .container
    = button_to 'Edit', user_edit_settings_path(@user), :class => 'btn btn-primary', :method => :get
    = render "shared/property_row", :header => "Password", :value => '(redacted)'
    = render "shared/property_row", :header => "Password Confirmation", :value => '(redacted)'
