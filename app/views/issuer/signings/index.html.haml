= render 'issuers/side_navigation', :active => 'issuer_newsfeed'

#information
  %h4
    Your Documents
  %hr
    - unless @signings.empty?
      - @signgings.each do |s|

        = render :partial => 'issuer/signings/signing', :signing => s
    - else
      %p
        There are no signings here right now.
